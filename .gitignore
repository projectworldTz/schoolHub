# Each subproject (backend/, frontend/, mobile/) has its own .gitignore for
# its own tooling. This one just covers OS/editor cruft at the repo root.
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# Written by deploy.sh on the server to track the last successfully
# deployed commit — not something to commit itself.
.last-deployed-commit
